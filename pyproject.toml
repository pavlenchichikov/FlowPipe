[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "flowpipe-etl"
version = "0.1.0"
description = "Visual ETL pipeline builder with drag-and-drop interface"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "FlowPipe Contributors" }]
keywords = ["etl", "pipeline", "data", "drag-and-drop", "visual"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Database",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "pandas>=2.0",
    "openpyxl>=3.1",
    "sqlalchemy>=2.0",
    "apscheduler>=3.10",
    "jinja2>=3.1",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "ruff>=0.4",
]
postgres = ["psycopg2-binary>=2.9"]
mysql = ["pymysql>=1.1"]

[project.scripts]
flowpipe = "flowpipe.__main__:main"

[project.urls]
Homepage = "https://github.com/yourname/flowpipe"
Repository = "https://github.com/yourname/flowpipe"
Issues = "https://github.com/yourname/flowpipe/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/flowpipe"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
